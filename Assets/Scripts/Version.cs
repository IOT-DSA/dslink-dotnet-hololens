using System;
using UnityEngine;
using UnityEngine.UI;

public class Version : MonoBehaviour {
	void Start()
	{
        //GetComponent<TextMesh>().text = Environment.Version.ToString();
	    GetComponent<TextMesh>().text = "test1";
	}
}
